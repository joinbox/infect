{% extends 'InfectBackendBundle::main.html.twig' %}


{% block javascript %}
{% endblock %}

{% block pageTitle %}
     {% trans %}Diagnosis{% endtrans %} 
{% endblock %}

{% block content %}

            <form action="{{ path('diagnosis_new') }}" method="POST">
            <button type="submit" class="btn btn-success">
                {% trans with {'%entity%': 'Diagnosis'|trans} %}%entity% hinzufügen{% endtrans %}
            </button>        
        </form>
    
    <table id="datatable" class="table table-striped table-hover">
        <thead>
            <tr>
                <th class="span1"></th>
                <th>{% trans %}Title{% endtrans %}</th> 
                <th>{% trans %}Country{% endtrans %}</th>
                <th>{% trans %}Topic{% endtrans %}</th>
                <th>{% trans %}Primary Therapy{% endtrans %}</th>
                <th>{% trans %}Bacterias{% endtrans %}</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td>
                    <div class="btn-group">
                        <a href="{{ path('diagnosis_edit', { 'id': entity.id }) }}" data-toggle="tooltip" title="{% trans %}editieren{% endtrans %}" class="btn btn-mini btn-success"><i class="icon-pencil"></i></a>
                        <a href="{{ path('diagnosis_show', { 'id': entity.id }) }}" data-toggle="tooltip" title="{% trans %}Details{% endtrans %}" class="btn btn-mini btn-info"><i class="icon-zoom-in"></i></a>
                    </div>
                </td>
                <td>
                    {% for locale in entity.locales %}
                        <span class="label label-info" style="min-width:25px;text-align:center;margin-right:5px;">{{ locale.language.name }}</span> {{ locale.title }}<br>
                    {% endfor %}
                </td>
                <td>
                    {{ entity.country }}
                </td>
                <td>
                    {{ entity.topic }}
                </td>
                <td>
                    {{ entity.idPrimarytherapy }}
                </td>
                <td>
                    {% if entity.bacterias|length > 0 %}
                    todo
                    {% endif %}
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>     

{% endblock %}


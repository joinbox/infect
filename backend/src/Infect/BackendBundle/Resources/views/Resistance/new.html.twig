    {% extends 'InfectBackendBundle::main.html.twig' %}



{% block javascript %}
{% endblock %}

{% block pageTitle %}
    {% trans with {'%entity%': 'Resistance'|trans} %}%entity% hinzufügen{% endtrans %}
{% endblock %}

{% block content %}

    <form class="form-horizontal form-box" method="post" action="{{ path('resistance_create') }}" {{ form_enctype(form) }}>
        <h4 class="form-box-header">
            {% trans %}Resistance{% endtrans %}
        </h4>

        <div class="form-box-content">
            {{ form_widget(form) }}
            {{ form_rest(form) }}

            <div class="form-actions">
                <input type="submit" class="btn btn-success" value="{% trans %}Hinzufügen{% endtrans %}">
                <a href="{{ path('resistance') }}">
                    <button type="button" class="btn btn-danger" value="">{% trans %}Abbrechen{% endtrans %}</button>
                </a>
            </div>

        </div>
    </form>
    
{% endblock %}


{% extends 'InfectBackendBundle::main.html.twig' %}


{% block javascript %}
{% endblock %}

{% block pageTitle %}
     {% trans %}Country{% endtrans %} 
{% endblock %}

{% block content %}

            <form action="{{ path('country_new') }}" method="POST">
            <button type="submit" class="btn btn-success">
                {% trans with {'%entity%': 'Country'|trans} %}%entity% hinzufügen{% endtrans %}
            </button>        
        </form>
    
    <table id="datatable" class="table table-striped table-hover">
        <thead>
            <tr>
                <th class="span1"></th>                                                                <th>Iso2</th>
                                                                <th>Iso3</th>
                                                                <th>{% trans %}Languages{% endtrans %}</th>
                                   
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                
                
                <td>
                    <div class="btn-group">
                        <a href="{{ path('country_edit', { 'id': entity.id }) }}" data-toggle="tooltip" title="{% trans %}editieren{% endtrans %}" class="btn btn-mini btn-success"><i class="icon-pencil"></i></a>
                        <a href="{{ path('country_show', { 'id': entity.id }) }}" data-toggle="tooltip" title="{% trans %}Details{% endtrans %}" class="btn btn-mini btn-info"><i class="icon-zoom-in"></i></a>
                    </div>
                </td>
                <td>{{ entity.iso2 }}</td>
                <td>{{ entity.iso3 }}</td>
                <td>
                    {% for language in entity.languages %}
                        <span class="label label-info" style="min-width:25px;text-align:center;margin-right:5px;">{{ language.name }}</span>
                    {% endfor %}
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>     

{% endblock %}


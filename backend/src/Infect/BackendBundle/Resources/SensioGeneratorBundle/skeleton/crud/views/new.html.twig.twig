{% block extends %}
    {{ "{% extends 'InfectBackendBundle::main.html.twig' %}" }}
{% endblock extends %}

{% block body %}

{{ "
{% block javascript %}
{% endblock %}

{% block pageTitle %}
    {% trans with {'%entity%': '"}}{{ entity }}{{"'|trans} %}%entity% hinzufügen{% endtrans %}
{% endblock %}" }}

{{ "{% block content %}" }}

    <form class="form-horizontal form-box" method="post" action="{{ "{{ path('" ~ route_name_prefix ~ "_create') }}" }}" {{ "{{ form_enctype(form) }}" }}>
        <h4 class="form-box-header">
            {{ "{% trans %}" }}{{ entity }}{{ "{% endtrans %}" }}
        </h4>

        <div class="form-box-content">
            {{ "{{ form_widget(form) }}" }}
            {{ "{{ form_rest(form) }}" }}

            <div class="form-actions">
                <input type="submit" class="btn btn-success" value="{{ "{% trans %}Hinzufügen{% endtrans %}" }}">
                <a href="{{ "{{ path('" ~ route_name_prefix ~ "') }}" }}">
                    <button type="button" class="btn btn-danger" value="">{{ "{% trans %}" }}Abbrechen{{ "{% endtrans %}" }}</button>
                </a>
            </div>

        </div>
    </form>
    
{{ "{% endblock %}" }}
{% endblock body %}

